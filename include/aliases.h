#ifndef ALIASES
#define ALIASES

#include <stdexcept>
#include <string>
#include <utility>

typedef std::string                             STRING;
typedef std::exception                          EXCEPTION;
typedef std::runtime_error                      RUNTIME_ERR;
typedef std::pair<std::string, std::string>     STRING_PAIR;

#endif
